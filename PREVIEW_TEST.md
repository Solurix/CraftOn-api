# Preview smoke test

Throwaway PR to validate the per-PR preview pipeline end-to-end (isolated
`crafton_pr<N>` database → boot migration → signup/login/me smoke test).

Safe to close/delete — no functional change.
